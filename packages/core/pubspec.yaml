name: core
description: "Core Utilties shared across all apps"
publish_to: "none"
version: 0.0.1

environment:
  sdk: ^3.12.1
  flutter: ">=1.17.0"

dependencies:
  equatable: ^2.1.0
  flutter:
    sdk: flutter
  fpdart: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
